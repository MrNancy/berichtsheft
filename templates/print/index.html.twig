{% block body %}
    <link rel="stylesheet" href="{{ asset('bundles/easyadmin/app.0bf72e55.css') }}">
    <style>
        @page {
            size: A4;
            margin: 0;
        }

        .page {
            width: 210mm;
            height: 297mm;
            padding: 2rem;
        }
    </style>
    {% set i = 0 %}
    {% for eintrag in eintraege %}
        {% set i = i + 1 %}
        <div class="page">
            {% include 'eintrag.html.twig' with {'eintrag':eintrag,'key':i} %}
        </div>
    {% endfor %}
{% endblock %}